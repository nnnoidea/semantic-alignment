# Realization Semantics

Realization semantics are intended artifact semantics: what the agent believes the produced skill, files, and records should mean after interpreting user semantics and filling implementation/design gaps.

| ID | Realization semantics | Scope | Relation to user semantics | Linked user semantics | Rationale | Status |
| --- | --- | --- | --- | --- | --- | --- |
| A1 | Keep the skill concise and procedural, with a reference template for durable records. | implementation | grounded | U1,U4,U5,U9 | Skill creation guidance prefers concise SKILL.md and separate references for reusable templates. | active |
| A2 | Replace `constraint-memory` instead of keeping both names. | implementation | added | U4,U9,U10 | Two similar skills would create trigger ambiguity and dilute the new concept. | active |
| A3 | Treat constraint-driven compromise as one semantic change reason inside the broader model. | global | grounded | U2,U3,U6 | User explicitly framed tool absence as one example, not the whole concept. | active |
| A4 | Install Python and Git globally from official installers, then use full paths in this Codex session until PATH refreshes. | implementation | grounded | U12 | User explicitly allowed admin/global installation; current process PATH does not refresh after installer updates machine PATH. | active |
| A5 | Split semantic records into multiple files under one task directory. | implementation | grounded | U11,U13 | The user identified centralization and source ambiguity as a semantic quality problem. | active |
| A6 | Make `user-semantics.md` a prose-first review document, with source notes after the summary. | implementation | grounded | U14 | The user said the user semantic record is the project gatekeeping entry point and must be readable by the user. | active |
| A7 | Replace standalone artifact semantics with artifact checks used to audit agent/intended artifact semantics. | global | grounded | U15,U16 | User clarified that agent semantics are intended artifact semantics; the risk is mismatch between agent belief and actual artifact. The artifact itself is the code/design/skill, so the record should contain checks rather than a third semantic layer. | active |
| A8 | Make `user-semantics.md` the current user-semantic state and move detailed user semantic changes to `user-semantic-ledger.md`. | implementation | grounded | U18 | User asked whether the user semantic record is final/current or detailed; separating current state from history preserves readability and traceability. | active |
| A9 | Add operational triggers with capture, clarify, classify, check, audit, reopen, revise, and escalate as skill behavior, not as a separate action log. | workflow | grounded | U18,U19 | User clarified that the missing action layer should tell the agent when and how to write files, not record actions as tasks. | active |
| A10 | Add project hygiene, confirmation, closure, and history-management rules to the skill. | workflow | grounded | U20 | User asked whether other practical gaps were changed and whether semantic records could pollute project files. | active |
| A11 | Represent repeated audits as a current summary plus append-only audit events with status, archiving old resolved events when needed. | workflow | grounded | U21 | User asked how repeated audit information should be recorded and whether it should be updated or deleted. | active |
| A12 | Make full audits user-initiated or user-accepted, while allowing agent-initiated audit recommendations and narrow proactive warnings for contradictions or clearly stale constraints. | workflow | grounded | U22 | User wants control over full audits, but also wants proactive reminders for semantic contradictions and possible route reopening. | active |
| A13 | Add required-read rules and observable trigger heuristics so semantic records are consulted before planning, risky inference, audit recommendations, constraint rechecks, and delivery. | workflow | grounded | U23 | User pointed out that records are useless if agents do not read them at the right moments. | active |
| A14 | Strengthen skill trigger metadata so non-trivial project/design/artifact tasks load semantic-alignment before planning or editing. | invocation | grounded | U23 | User wants the skill loaded before any project work; trigger metadata is the practical mechanism available to a skill. | active |
| A15 | Add a semantic capture filter: record only user statements that affect project/design/artifact semantics or this skill's semantic-recording behavior; keep process-only checks out of user semantic history. | workflow | grounded | U25 | User clarified that not every user input should be recorded as user semantics. | active |
| A16 | Replace source-note user history with a user semantic ledger using add/update/delete operations, semantic categories, before/after states, reasons, source, current flag, and recheck trigger. | workflow | grounded | U15,U16,U17 | User clarified that history must show A-to-B semantic changes with reasons, while the user-facing file is the latest projected state. | active |
| A17 | Add a validation script for semantic-alignment record directories to enforce file presence, table headers, and controlled vocabulary. | implementation | grounded | U15,U16,U17 | User asked whether records should rely on agent-written Markdown or use scripts to fix/check format. | active |
| A18 | Add an initialization script for semantic-alignment record directories and tighten validation to include `index.md` and semantic-delta reason values. | implementation | grounded | U18 | Fixed skeleton generation reduces format drift while keeping semantic judgment with agent/user. | active |
| A19 | Make `.semantic-alignment/<task-slug>/` the platform-neutral default record path and remove `.codex` as a documented option. | workflow | grounded | U19 | Binding records to a Codex-specific directory would limit reuse on other agent platforms and can create permission problems. | active |
| A20 | Rename the intended-artifact semantic layer from agent semantics to realization semantics, including the record file name and audit lens names. | workflow | grounded | U20 | The new name better describes semantics being realized in artifacts and reduces agent-subject bias. Existing historical IDs are preserved to avoid churn. | active |
| A21 | Keep `user-semantics.md` Markdown, but allow non-user-facing records to move to YAML/JSON/JSONL when stronger parsing is useful. | workflow | grounded | U21 | User clarified that user-readable files need Markdown, while other files can be made more robust. | active |
| A22 | Define `user-semantic-ledger.md` as user-baseline history and `semantic-deltas.md` as cross-layer semantic transition history. | workflow | grounded | U22 | Superseded by A24 after user clarified the ledger should own the practical action hook. | revised |
| A23 | Expand semantic delta sources so agent responses, suggestions, limitations, and compromises can create deltas even without new user input. | workflow | grounded | U23 | Superseded by A25 after user clarified unaccepted no-impact agent responses should not be persisted. | revised |
| A24 | Remove `semantic-deltas.md` and make `user-semantic-ledger.md` the action-bearing history for accepted user-baseline changes and recheck triggers. | workflow | grounded | U24 | User clarified that the practical condition-change/project-direction role belongs in the user semantic ledger. | active |
| A25 | Do not persist unaccepted no-impact agent suggestions or limitations in semantic records. | workflow | grounded | U25 | User clarified that if an agent response has no accepted or actual effect, it has no semantic record value. | active |
| A26 | Replace unconditional rereading with a freshness model: load the semantic frame at task start, reuse it only when it is known fresh, and reread on staleness signals. | workflow | grounded | U26 | User clarified that rereading every time is unnecessary when the current semantic baseline is already in context. | active |
| A27 | Add `recheck-triggers.md` as a compact current projection of active/unverified ledger triggers, validated and generated with the record scripts. | workflow | grounded | U27 | User wants timely trigger checks without scanning the full ledger on every request. | active |
| A28 | Add `scripts/sync_triggers.py` so `recheck-triggers.md` is generated from current ledger rows, preserving existing check metadata for matching triggers; make validation detect stale trigger projections. | implementation | grounded | U28 | User said script generation would be more stable than relying on agent-maintained trigger rows. | active |
| A29 | Refactor `SKILL.md` into a concise core workflow and move detailed semantic model, operational workflow, and audit rules into focused reference files loaded on demand. | implementation | grounded | U29 | User identified the current skill body as too long; progressive disclosure better matches skill-creator guidance and reduces default context cost. | active |
| A30 | Align ledger reason vocabulary across semantic model, record template, and validator by allowing `implementation-discovery` and `agent-inference` everywhere. | implementation | grounded | U18,U21 | This fixes the audit-found mismatch without changing user-facing semantics. | active |
| A31 | Define recheck triggers as observable conditions that cause the linked ledger entry to be rechecked, and clean current ledger triggers to remove open tasks, recommendations, and actions. | workflow | grounded | U30 | User clarified the intended trigger semantics and identified the current trigger projection as conceptually wrong. | active |
| A32 | Add a startup checklist, stricter reference-loading instructions, and `scripts/lint_records.py` for semantic-quality warnings beyond structural validation. | implementation | grounded | U31 | This reduces reliance on agent self-discipline and catches likely record-quality issues that validation cannot judge. | active |
| A33 | Validate standard record table IDs for duplicates so generated or manually edited records cannot silently contain conflicting entries. | implementation | grounded | U18,U31 | Forward validation exposed a duplicate realization ID that existing validation missed; duplicate IDs weaken traceability and should be mechanically rejected. | active |
| A34 | Make stale-route reminders user-visible: when a recheck trigger becomes true, the agent must state the prior compromise and current condition before resolving the trigger or proceeding with an explicitly confirmed old route. | workflow | grounded | U32 | The sub-agent stale-route test showed records could be updated correctly while the user-facing reminder was too implicit. | active |
| A35 | Provide scripts/record_event.py as the preferred low-freedom path for appending ledger, realization, and artifact-check rows with generated IDs and enum checks. | implementation | grounded | U33 | Scripted row appends reduce Markdown table drift while preserving agent/user semantic judgment. | active |
| A36 | Generate recheck-triggers.md with deduplicated trigger rows, joining linked ledger IDs when the observable condition is identical. | implementation | grounded | U34 | Duplicate trigger rows add monitoring noise without adding distinct recheck conditions. | active |
| A37 | Provide scripts/export_structured.py to generate JSONL mirrors for non-user-facing records without replacing user-semantics.md as the Markdown review entry point. | implementation | grounded | U35 | This gives stronger machine-readable data gradually without forcing a disruptive source-format migration. | active |
| A38 | Keep reference splitting conditional on distinct loading moments; avoid splitting references that agents would still load together for the same action. | workflow | grounded | U36 | Splitting only improves progressive disclosure when it changes what agents actually need to read. | active |
| A39 | Package the GitHub repository with README.md and examples/ as documentation-level files while keeping SKILL.md, references/, scripts/, and agents/ as the runtime skill surface. | implementation | grounded | U37 | The user wants examples uploaded with the repo but not treated as part of the skill runtime. | active |
| A40 | Publish-facing README files should be split by language and explain the user problem before the solution, without exposing internal script mechanics as primary user-facing instructions. | content | grounded | U38 | The user corrected the README communication structure and Chinese tone. | active |
| A41 | The English README should mirror the Chinese README's semantic structure and user-facing framing rather than diverging into a separate technical explanation. | content | grounded | U39 | User clarified that English should express the same semantics as the Chinese README. | active |
| A42 | Chinese README should retain the problem-first structure while using restrained, publication-ready Chinese rather than a strongly conversational style. | content | grounded | U40 | User requested a slight tone adjustment for Chinese README. | active |
