# User Semantics

This is the complete current user-design baseline. It is generated from `semantic-ledger.jsonl`; edit the ledger through `scripts/record_event.py`.

## Goals

- `U1` Users can export the report as a file. _(related: U2)_

## Principles

- `U2` Report content must not be uploaded to external services. _(related: U1)_
