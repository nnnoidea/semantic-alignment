interface:
  display_name: "Semantic Alignment"
  short_description: "Keep durable product and system implementation aligned with user design."
  default_prompt: "Use $semantic-alignment for durable product or system design work: preserve current user semantics through tools, audit real artifacts incrementally, report differences, and retain compromises. Skip routine experiments and ordinary writing unless explicitly requested or they define a lasting implementation contract."
